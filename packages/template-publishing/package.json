{
  "name": "@cloudcrane/template-publishing",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --config ../../eslint.config.mjs .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --exclude dist/** --exclude .next/**"
  },
  "dependencies": {
    "@cloudcrane/db": "workspace:*",
    "@cloudcrane/pboot-snapshot": "workspace:*",
    "@cloudcrane/shared": "workspace:*",
    "@cloudcrane/workspace-client": "workspace:*",
    "drizzle-orm": "0.45.2"
  }
}
