{
  "name": "@cloudcrane/workspace-protocol",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint --config ../../eslint.config.mjs .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude dist/** --exclude .next/**"
  },
  "dependencies": {
    "zod": "4.5.4"
  }
}
